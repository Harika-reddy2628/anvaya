import Default from "@/components/ui/demo";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Agent Dock Demo | Anvaya",
  description: "Interactive demo of the AgentDock component",
};

export default function AgentDockPage() {
  return <Default />;
}
